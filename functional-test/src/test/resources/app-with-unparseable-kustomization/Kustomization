bases:
  - invalid-kustomization-base                    # Referencing the kustomization file directly
  - valid-kustomization-base                      # Referencing the directory (should pick up kustomization.yaml inside)