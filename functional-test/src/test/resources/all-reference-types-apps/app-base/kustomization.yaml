bases:
  - base-kustomization-reference/kustomization.yaml  # Referencing the kustomization file directly
  - base-directory-reference                      # Referencing the directory (should pick up kustomization.yaml inside)