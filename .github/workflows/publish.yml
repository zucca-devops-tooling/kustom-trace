name: Publish

on:
  push:
    branches:
      - "**"

env:
  GRADLE_OPTS: -Dorg.gradle.jvmargs="-Xmx2g -XX:+HeapDumpOnOutOfMemoryError"

jobs:
  verify:
    name: Build and Verify
    if: >-
      contains(github.event.head_commit.message || '', 'publishLib') ||
      contains(github.event.head_commit.message || '', 'publishCli') ||
      contains(github.event.head_commit.message || '', 'publishJarCli') ||
      contains(github.event.head_commit.message || '', 'publishLinCli') ||
      contains(github.event.head_commit.message || '', 'publishWinCli') ||
      contains(github.event.head_commit.message || '', 'publishMacCli') ||
      contains(github.event.head_commit.message || '', 'publishAll') ||
      (github.ref_name == 'main' && contains(github.event.head_commit.message || '', 'release'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Make Gradle wrapper executable
        run: chmod +x gradlew

      - name: Build
        run: ./gradlew clean assemble --info --no-daemon

      - name: Spotless
        run: ./gradlew check -x test --no-daemon

      - name: Unit tests
        run: ./gradlew :kustomtrace:test --no-daemon

      - name: Functional tests
        run: ./gradlew :functional-test:test --no-daemon --info

  publish-library:
    name: Publish Library
    needs: verify
    if: >-
      needs.verify.result == 'success' &&
      (
        contains(github.event.head_commit.message || '', 'publishLib') ||
        contains(github.event.head_commit.message || '', 'publishAll') ||
        (github.ref_name == 'main' && contains(github.event.head_commit.message || '', 'release'))
      )
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      # If the token belongs to a service user, set GH_PACKAGES_USERNAME as an org/repo variable.
      GH_PACKAGES_USERNAME: ${{ vars.GH_PACKAGES_USERNAME || github.actor }}
      GH_PACKAGES_TOKEN: ${{ secrets.GH_PACKAGES_TOKEN }}
      GPG_KEY_ID: ${{ secrets.GPG_KEY_ID }}
      GPG_KEY_PASS: ${{ secrets.GPG_KEY_PASS }}
      OSSRH_USERNAME: ${{ secrets.OSSRH_USERNAME }}
      OSSRH_PASSWORD: ${{ secrets.OSSRH_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Make Gradle wrapper executable
        run: chmod +x gradlew

      - name: Write GPG key
        run: printf '%s' "${{ secrets.GPG_SECRET_KEY }}" > "$RUNNER_TEMP/gpg-secret-key.asc"

      - name: Publish library
        env:
          GPG_KEY_PATH: ${{ runner.temp }}/gpg-secret-key.asc
        run: |
          ./gradlew :kustomtrace:publish --info --no-daemon \
            -Psigning.keyId="${GPG_KEY_ID}" \
            -Psigning.password="${GPG_KEY_PASS}" \
            -Psigning.secretKeyRingFile="${GPG_KEY_PATH}" \
            -PgithubPackagesUsername="${GH_PACKAGES_USERNAME}" \
            -PgithubPackagesPassword="${GH_PACKAGES_TOKEN}" \
            -PmavenCentralUsername="${OSSRH_USERNAME}" \
            -PmavenCentralPassword="${OSSRH_PASSWORD}"

  publish-cli-jar:
    name: Publish CLI JAR
    needs: verify
    if: >-
      needs.verify.result == 'success' &&
      (
        contains(github.event.head_commit.message || '', 'publishCli') ||
        contains(github.event.head_commit.message || '', 'publishJarCli') ||
        contains(github.event.head_commit.message || '', 'publishAll') ||
        (github.ref_name == 'main' && contains(github.event.head_commit.message || '', 'release'))
      )
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      # If the token belongs to a service user, set GH_PACKAGES_USERNAME as an org/repo variable.
      GH_PACKAGES_USERNAME: ${{ vars.GH_PACKAGES_USERNAME || github.actor }}
      GH_PACKAGES_TOKEN: ${{ secrets.GH_PACKAGES_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Make Gradle wrapper executable
        run: chmod +x gradlew

      - name: Publish CLI JAR
        run: |
          ./gradlew :kustomtrace-cli:publish --info --no-daemon \
            -PgithubPackagesUsername="${GH_PACKAGES_USERNAME}" \
            -PgithubPackagesPassword="${GH_PACKAGES_TOKEN}"

  publish-cli-native-linux:
    name: Publish CLI Native Linux
    needs: verify
    if: >-
      needs.verify.result == 'success' &&
      (
        contains(github.event.head_commit.message || '', 'publishCli') ||
        contains(github.event.head_commit.message || '', 'publishLinCli') ||
        contains(github.event.head_commit.message || '', 'publishAll') ||
        (github.ref_name == 'main' && contains(github.event.head_commit.message || '', 'release'))
      )
    permissions:
      contents: read
    uses: ./.github/workflows/publish-native-linux.yml
    secrets: inherit

  publish-cli-native-windows:
    name: Publish CLI Native Windows
    needs: verify
    if: >-
      needs.verify.result == 'success' &&
      (
        contains(github.event.head_commit.message || '', 'publishCli') ||
        contains(github.event.head_commit.message || '', 'publishWinCli') ||
        contains(github.event.head_commit.message || '', 'publishAll') ||
        (github.ref_name == 'main' && contains(github.event.head_commit.message || '', 'release'))
      )
    permissions:
      contents: read
    uses: ./.github/workflows/publish-native-windows.yml
    secrets: inherit

  publish-cli-native-macos:
    name: Publish CLI Native macOS
    needs: verify
    if: >-
      needs.verify.result == 'success' &&
      (
        contains(github.event.head_commit.message || '', 'publishCli') ||
        contains(github.event.head_commit.message || '', 'publishMacCli') ||
        contains(github.event.head_commit.message || '', 'publishAll') ||
        (github.ref_name == 'main' && contains(github.event.head_commit.message || '', 'release'))
      )
    permissions:
      contents: read
    uses: ./.github/workflows/publish-native-macos.yml
    secrets: inherit

  release:
    name: Tag and Release
    needs:
      - publish-library
      - publish-cli-jar
      - publish-cli-native-linux
      - publish-cli-native-windows
      - publish-cli-native-macos
    if: >-
      needs.publish-library.result == 'success' &&
      needs.publish-cli-jar.result == 'success' &&
      needs.publish-cli-native-linux.result == 'success' &&
      needs.publish-cli-native-windows.result == 'success' &&
      needs.publish-cli-native-macos.result == 'success' &&
      github.ref_name == 'main' &&
      contains(github.event.head_commit.message || '', 'release')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Make Gradle wrapper executable
        run: chmod +x gradlew

      - name: Build CLI release artifact
        run: ./gradlew :kustomtrace-cli:shadowJar --info --no-daemon

      - name: Download Linux native artifact
        uses: actions/download-artifact@v4
        with:
          name: kustomtrace-cli-linux-x64
          path: release-assets

      - name: Download Windows native artifact
        uses: actions/download-artifact@v4
        with:
          name: kustomtrace-cli-windows-x64
          path: release-assets

      - name: Download macOS native artifact
        uses: actions/download-artifact@v4
        with:
          name: kustomtrace-cli-macos-x64
          path: release-assets

      - name: Tag release
        run: ./gradlew tagRelease --no-daemon

      - name: Read release metadata
        id: release
        run: |
          release_version="$(./gradlew properties -q -Pquiet | awk '/^version:/ { print $2 }')"
          if [[ -z "$release_version" ]]; then
            echo "Failed to read project version." >&2
            exit 1
          fi

          changelog_notes="$(awk "/^## \\[$release_version\\]/{flag=1; next} /^## \\[/{flag=0} flag" CHANGELOG.md)"
          if [[ -z "$changelog_notes" ]]; then
            changelog_notes="No specific changelog notes found for this version."
          fi

          shadow_jar_path="$(find cli/build/libs -name 'kustomtrace-cli-*-all.jar' -print -quit)"
          if [[ -z "$shadow_jar_path" ]]; then
            echo "CLI Shadow JAR not found after build." >&2
            exit 1
          fi

          native_linux_archive="release-assets/kustomtrace-linux-x64.tar.gz"
          if [[ ! -f "$native_linux_archive" ]]; then
            echo "Linux native archive not found after artifact download." >&2
            exit 1
          fi

          native_linux_checksum="release-assets/kustomtrace-linux-x64.tar.gz.sha256"
          if [[ ! -f "$native_linux_checksum" ]]; then
            echo "Linux native checksum not found after artifact download." >&2
            exit 1
          fi

          native_windows_archive="release-assets/kustomtrace-windows-x64.zip"
          if [[ ! -f "$native_windows_archive" ]]; then
            echo "Windows native archive not found after artifact download." >&2
            exit 1
          fi

          native_windows_checksum="release-assets/kustomtrace-windows-x64.zip.sha256"
          if [[ ! -f "$native_windows_checksum" ]]; then
            echo "Windows native checksum not found after artifact download." >&2
            exit 1
          fi

          native_macos_archive="release-assets/kustomtrace-macos-x64.tar.gz"
          if [[ ! -f "$native_macos_archive" ]]; then
            echo "macOS native archive not found after artifact download." >&2
            exit 1
          fi

          native_macos_checksum="release-assets/kustomtrace-macos-x64.tar.gz.sha256"
          if [[ ! -f "$native_macos_checksum" ]]; then
            echo "macOS native checksum not found after artifact download." >&2
            exit 1
          fi

          notes_file="$RUNNER_TEMP/release-notes.md"
          printf '%s\n' "$changelog_notes" > "$notes_file"

          {
            echo "tag_name=v$release_version"
            echo "shadow_jar_path=$shadow_jar_path"
            echo "native_linux_archive=$native_linux_archive"
            echo "native_linux_checksum=$native_linux_checksum"
            echo "native_windows_archive=$native_windows_archive"
            echo "native_windows_checksum=$native_windows_checksum"
            echo "native_macos_archive=$native_macos_archive"
            echo "native_macos_checksum=$native_macos_checksum"
            echo "notes_file=$notes_file"
          } >> "$GITHUB_OUTPUT"

      - name: Push tag
        env:
          TAG_NAME: ${{ steps.release.outputs.tag_name }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git push origin "${TAG_NAME}"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_NAME: ${{ steps.release.outputs.tag_name }}
          SHADOW_JAR_PATH: ${{ steps.release.outputs.shadow_jar_path }}
          NATIVE_LINUX_ARCHIVE: ${{ steps.release.outputs.native_linux_archive }}
          NATIVE_LINUX_CHECKSUM: ${{ steps.release.outputs.native_linux_checksum }}
          NATIVE_WINDOWS_ARCHIVE: ${{ steps.release.outputs.native_windows_archive }}
          NATIVE_WINDOWS_CHECKSUM: ${{ steps.release.outputs.native_windows_checksum }}
          NATIVE_MACOS_ARCHIVE: ${{ steps.release.outputs.native_macos_archive }}
          NATIVE_MACOS_CHECKSUM: ${{ steps.release.outputs.native_macos_checksum }}
          NOTES_FILE: ${{ steps.release.outputs.notes_file }}
        run: |
          gh release create "${TAG_NAME}" \
            "${SHADOW_JAR_PATH}" \
            "${NATIVE_LINUX_ARCHIVE}" \
            "${NATIVE_LINUX_CHECKSUM}" \
            "${NATIVE_WINDOWS_ARCHIVE}" \
            "${NATIVE_WINDOWS_CHECKSUM}" \
            "${NATIVE_MACOS_ARCHIVE}" \
            "${NATIVE_MACOS_CHECKSUM}" \
            --title "Release ${TAG_NAME}" \
            --notes-file "${NOTES_FILE}"
